# Defaults for Graphite carbon-daemons - sourced by
# /etc/init.d/carbon-cache & /etc/init.d/carbon-relay

# Instances
CACHE_INSTANCES="<%= @cache_instances %>"
RELAY_INSTANCES="<%= @relay_instances %>"
